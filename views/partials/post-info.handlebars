<article class="post">
  <div class="title">
    <span>{{title}}
      by
      {{user.username}}
      on
      {{created_at}}
      |</span>
  </div>
  <div class="meta">
    {{content}}
    <a href="/post/{{id}}">{{comments.length}} {{"comment" comments.length}}</a>
  </div>
</article>